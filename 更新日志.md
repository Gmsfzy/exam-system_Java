# 智汇学场（Java 版） — 一体化智能学习平台 更新日志

> 记录 Java/Spring Boot 重构版从搭建到当前版本的每一次关键改动，语义对标 Python 版 `exam_system_Beta`，字段与实现按 Java camelCase / JPA 风格落地。

---

## 版本里程碑索引

| 里程碑 | 主题 | 说明 |
|--------|------|------|
| M1 | 平台骨架 | Spring Boot 3 + Vue 3 四模块门户、JWT 认证、主题、通知、限流 |
| M2 | 在线考试内核 | 题库/组卷/答题执行/客观题判分/主观题 AI 异步判分/人工阅卷/成绩分析 |
| M3 | 自主学习 | 错题本、自由练习、学习计划、学情报告、AI 答疑 |
| M4 | 征集悬赏 | 发布/投稿/采纳（CAS 抢锁）/赏金结算 |
| M5 | 答题竞赛 + 竞技化 | 限时赛、1v1 PK、段位赛季、徽章、学情画像、战队 |
| **M6** | **考务升级（对标 Python v4.3）** | **多轮次考试、成绩策略、A/B 卷与选项乱序、多选分级给分、匿名阅卷、成绩发布门控、实时监考、成绩导出、试题分析、AI 质检、材料题、成绩申诉** |

---

## M6 考务升级（对标 Python v4.3）

### 目标与决策

- 本轮只做 **A 考务升级包**；B 悬赏治理（v4.2）、C 学习社区（v4.1/4.4/4.5）、D 竞赛内核（v4.1/4.11）留作 M7+ 迭代。
- 无迁移脚本：新列由 JPA `ddl-auto: update` 自动补齐，存量数据默认值保持兼容（`resultsPublished=true`、`maxAttempts=1`、`paperMode=unified`）。

### 后端：实体加列

- `Exam`：新增 `resultsPublished`（默认 true）、`maxAttempts`（默认 1）、`scoreStrategy`（last/best，默认 last）、`paperMode`（unified/random）、`randomCount`、`shuffleOptions`、`multiScoreRule`（all_or_nothing/partial）、`anonymousGrading`；提供 `*Effective()` 兜底方法应对存量 null。
- `Question`：新增 `material`（TEXT 共享材料）、`materialGroup`（同组标识字符串）。
- `ExamSession`：新增 `attemptNo`（默认 1）、`score`、`assignedQuestionIds`（JSON，复用 `JsonLongListConverter`）、`optionMap`（JSON，扁平结构 `questionId:显示字母 -> 原字母`）。
- `Result`：新增 `sessionId`、`reviewStatus`（none/pending/approved/rejected）、`reviewReason`、`reviewReply`、`reviewedAt`；`ResultRepository` 增加按会话/轮次查询与 `findEffective` 聚合语义。

### 后端：执行链路改造（`ExamExecutionService`）

- `start`：校验考次（已用轮次 + 补考授权 ≤ maxAttempts，超限报错）；`paperMode=random` 时按 `randomCount`（0=全部）从试卷抽题写 `assignedQuestionIds`；`shuffleOptions=true` 时对单选/多选生成会话级 `optionMap`。
- `take`：按会话 `assignedQuestionIds` 出题、按 `optionMap` 重排选项字母下发（学生端零感知）。
- `submit`：先按 `optionMap` 把学生选项映射回原字母再判分；多选按 `multiScoreRule`（partial=漏选得 `答对数/正确选项数×分值` 且含错选 0 分）；写 `session.score`；成绩落库按 `scoreStrategy` 以 `sessionId` 归属做 upsert。
- 判分原语抽取到 `util/ExamScoringUtil`（`displayToOriginal`/`originalToDisplay`/`scoreMultiChoice`），并修复了 `originalToDisplay` 逆映射的 `questionId:` 前缀不匹配缺陷。

### 后端：成绩服务（`ResultService` / `SubjectiveGradingService`）

- 发布门控：学生视角成绩接口在 `resultsPublished=false` 时总分/详情置 null + `published=false` 标志；通知同样门控。
- 匿名阅卷：`anonymousGrading=true` 时阅卷列表学生身份以「考生NN」化名下发（教师提交评分仍按真实 studentId 路由）。
- 申诉：`requestReview`（学生发起，置 pending）/ `handleReview`（教师 approve 带 newScore 重算总分 / reject 带 reply，写回复 + 站内通知）。
- 聚类批注 / 同题批量给分：`gradingCluster`（未批主观题按题聚合）、`batchGradeQuestion`（同题统一给分）。

### 后端：新增端点（10 个）

- `ExamController`：`POST /exams/{id}/publish_results`、`POST /exams/{id}/grant_attempt`、`GET /exams/{id}/monitor`、`GET /exams/{id}/results/export`（POI xlsx 流）、`GET /exams/{id}/item_analysis`（得分率/区分度=高前27%减低后27%/干扰项选择率）、`POST /exams/{id}/ai_inspect`（整卷题清送 `ArkAiClient`）。
- `ResultController`：`POST /results/{resultId}/review`、`POST /results/{resultId}/review_handle`、`GET /results/grading/{examId}/cluster`、`POST /results/grading/{examId}/question_batch`。
- 判分/组卷既有接口无新端点，新字段并入 `ExamDto` 的 create/update/take。

### 前端（Vue 3 + Element Plus）

- `ExamManage.vue`：新增「考务配置」标签页（8 个新字段表单）、顶部「发布成绩」「实时监考」「试题分析」入口、考生列表逐行「补考授权」对话框。
- `Exams.vue`：列表新增「成绩」发布状态标记列。
- `TakeExam.vue`：材料题渲染（同 `materialGroup` 顶部共享材料卡）；乱序/抽题对透明。
- 新页 `ExamMonitor.vue`（路由 `/exams/:id/monitor`）：10s 轮询监考表 + 成绩导出下载；新页 `ExamAnalysis.vue`（路由 `/exams/:id/analysis`）：试题分析表 + AI 质检结果卡。
- `Grading.vue`：逐人批注 / 聚类批注视图切换 + 同题批量给分；匿名化名展示。
- `MyResults.vue` / `ResultDetail.vue`：轮次列表、「未发布」占位、成绩申诉按钮 + 状态徽标 + 教师处理（approve 改分 / reject）。
- `Questions.vue`：material/materialGroup 编辑项；`router/index.js` 两条新路由；`App.vue` 考试模块侧栏在某场考试上下文动态补「监考」「分析」入口。

### 测试

- 单测（H2）：`ExamScoringUtilTest`（多选判分矩阵、`optionMap` 正反映射、无乱序透传）；`SubjectiveGradingServiceTest` 适配按会话（`findBySessionId`）收尾。
- 集成测试：`ExamOpsM6IntegrationTest` 全链——发布（maxAttempts=2/best/不自动发布）→ 两轮交卷 → 门控 → 三轮上限拦截 → publish_results → monitor / item_analysis / xlsx 导出（PK 魔数校验）→ 申诉 → 教师改分 approve → 总分重算；另含 grant_attempt 上限上调。
- 后端 `mvn test` 全绿（56 项）；前端 `npm run build` 通过；浏览器冒烟验证考务配置保存、监考表、试题分析页与 AI 质检按钮均正常渲染。
- 冒烟修复：`App.vue` `menuItems` 计算属性中 `base` 由 `const` 改为 `let`（修复 `:id` 路由下「Assignment to constant variable」导致监考/分析页空白）。

### 文档修订

- `说明文档.md`：新增「6.7 考务执行引擎（M6）」小节（start/take/submit 链路、`ExamScoringUtil` 判分原语、best/last 有效成绩、发布门控、匿名化名、区分度算法）；订正 `grant_attempt`（上调考试级 `maxAttempts`，默认 +1 封顶 100）、`review_handle`（approve 按 newScore 直接改分）、best/last（每轮各存一条 `Result` + `findEffective` 取有效成绩、非覆盖）语义；`JWT_SECRET` 说明由「prod 必填」更正为「所有环境必填（`@PostConstruct` 校验）」，快速启动与排错节同步补环境变量示例。
- `docs/postman/exam-system.postman_collection.json`：新增「16-考务升级（M6）」文件夹（10 个端点 + 含 M6 配置的建考请求），补 `resultId` 集合变量。

### 后续路线（本轮不做，逐个立项）

- M7 悬赏社区治理（v4.2）：编辑/撤回/多档采纳/讨论投票/钱包托管/定向/举报/查重。
- M8 学习体验与社区（v4.1/4.4/4.5）：知识点/学习路径/SM-2/AI 助手 SSE/归因/番茄钟/笔记/资料库/关注/AI 配额。
- M9 竞赛内核与赛事（v4.1/4.5/4.11）：Elo/系列赛/回放/证书/门禁/提醒/作品赛/DS 赛。
- M10 平台基建（v4.6/4.7/4.8）：统一上传白名单/RBAC 收敛/编程题 OJ/统一账本。

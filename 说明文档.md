# 智汇学场 · 一体化智能学习平台

> 基于 Spring Boot 3 + Vue 3 的在线学习考试系统，包含 **在线考试、自主学习、答题竞赛、征集悬赏** 四大模块，内置豆包（火山引擎方舟）大模型支持 AI 出题、智能组卷、异步主观题判分、考试分析与学习答疑；支持 JWT 认证、WebSocket 实时推送、亮暗双主题与 SQLite / SQL Server 双数据库环境。

---

## 目录

- [一、功能概览](#一功能概览)
- [二、技术栈](#二技术栈)
- [三、目录结构](#三目录结构)
- [四、快速启动（开发）](#四快速启动开发)
- [五、配置说明（多环境）](#五配置说明多环境)
- [六、后端架构](#六后端架构)
- [七、前端架构](#七前端架构)
- [八、数据库结构](#八数据库结构)
- [九、REST API 参考](#九rest-api-参考)
- [十、AI 能力与调用链路](#十ai-能力与调用链路)
- [十一、测试、构建与部署](#十一测试构建与部署)
- [十二、常见问题与排错](#十二常见问题与排错)
- [十三、历史遗留说明](#十三历史遗留说明)

---

## 一、功能概览

登录后先进入**门户中枢**，从四大模块中选择进入，各模块拥有独立侧边栏；顶栏常驻模块切换器、通知中心与亮暗主题开关。

### 模块一：在线考试

| 功能 | 角色 | 说明 |
|------|------|------|
| 题库管理 | 教师 | 9 种题型、难度分级、按专业/课程/章节分类；增删改查、公开共享开关 |
| AI 出题 / 智能组卷 | 教师 | 豆包生成题目与解析；按设置自动组卷 |
| 考试管理 | 教师 | 创建/编辑考试、配置分值、发布/结束、邀请码添加考生 |
| Excel 题库导入 | 教师 | Apache POI 解析批量导入 |
| 考生答题 | 学生 | 在线答题、自动保存、倒计时、切屏上报、交卷 |
| 客观题自动判分 + 主观题 AI 异步判分 | 系统 | 交卷事务提交后异步调用 AI，不阻塞交卷 |
| 人工阅卷 | 教师 | 对主观题复核打分、写评语 |
| 成绩与考试分析 | 教师/学生 | 成绩列表、单题作答详情、得分分布、整场 AI 诊断报告 |

### 模块二：自主学习（师生同权）

| 功能 | 说明 |
|------|------|
| 错题本 | 汇集考试/练习/竞赛错题，标记已掌握、移除 |
| 自由练习 | 选题组卷练习、即时判分、历史记录 |
| 学习计划 | 创建/编辑/暂停计划，跟踪进度 |
| 学情报告 | 学习日志统计、每日打卡、总体概览 |
| AI 答疑 | 针对薄弱知识点向 AI 提问 |

### 模块三：答题竞赛 + 竞技化

| 功能 | 角色 | 说明 |
|------|------|------|
| 竞赛管理 | 教师 | 创建竞赛、选题、发布/结束、查看参与者 |
| 竞赛大厅 / 参赛 | 学生 | 报名、限时作答、交卷即时判分（速度加权） |
| 实时排行榜 | 全部 | WebSocket 推送榜单变化 |
| 1v1 PK | 学生 | 大厅匹配/邀请、同题对战、实时状态推送 |
| 段位与阶梯 | 全部 | 赛季积分、段位、历史档案查询 |
| 战队 | 全部 | 创建/加入/退出/转让/解散 |
| 徽章 / 学情画像 | 全部 | 成就徽章、五维能力雷达图（Chart.js，主题感知） |

### 模块四：征集悬赏（师生同权）

| 功能 | 说明 |
|------|------|
| 发布悬赏 | 面向题目/答案等征集，设置赏金 |
| 投稿 | 其他用户提交作品；采纳后投稿内容可入库题库（source=bounty） |
| 采纳/驳回 | 发布人 CAS 抢锁采纳，赏金结算（防并发重复采纳） |
| 我的发布 / 我的投稿 | 进度与结果跟踪 |

### 平台级特性

- 亮暗主题全局切换（持久化 + 跟随系统）
- 站内通知（WebSocket 实时推送）
- 操作日志采集（前端 + 后端）
- Bucket4j 接口限流
- Swagger / OpenAPI 在线文档
- 统一响应格式与全局异常处理

---

## 二、技术栈

### 后端

| 组件 | 选型 | 说明 |
|------|------|------|
| 语言 | Java 17 | pom 已覆盖 Lombok/Mockito 版本以兼容 JDK 24/25 |
| 框架 | Spring Boot 3.3.5 | web / validation / security / websocket starters |
| 数据访问 | Spring Data JPA (Hibernate) | `ddl-auto: update` 自动建表；社区方言支持 SQLite |
| 数据库 | SQLite（开发默认）/ SQL Server（生产）/ H2（测试） | 通过 profile 切换，见[第五节](#五配置说明多环境) |
| 认证 | Spring Security + JWT（jjwt 0.12.6） | BCrypt 密码哈希，Bearer Token |
| 实时通信 | Spring WebSocket + STOMP | 端点 `/ws`（SockJS 与原生双注册），JWT 握手拦截 |
| 限流 | Bucket4j 8.10.1 | 内存令牌桶，`RateLimitFilter` |
| Excel | Apache POI 5.3.0 | 题库批量导入 |
| API 文档 | springdoc-openapi 2.6.0 | `/swagger-ui.html`（生产关闭） |
| AI 服务 | 豆包大模型（火山引擎方舟） | `ArkAiClient`，RestTemplate + 超时/重试 |
| 其他 | Lombok | DTO 大量使用 record |

### 前端

| 组件 | 选型 | 说明 |
|------|------|------|
| 构建工具 | Vite 5 | dev 端口 5173，`/api`、`/ws` 代理到 8080 |
| 框架 | Vue 3 | Composition API + `<script setup>` |
| 路由 | Vue Router 4 | `createWebHistory`，meta 驱动权限与模块归属 |
| 状态管理 | Pinia | `stores/auth.js`（登录态）、`stores/theme.js`（主题） |
| UI 组件 | Element Plus 2.14.2 | 含官方暗色 `theme-chalk/dark/css-vars.css` |
| HTTP 客户端 | Axios | 统一实例 + Bearer 拦截器 |
| 图表 | Chart.js | 五维能力雷达图（主题感知重绘） |
| WebSocket | @stomp/stompjs | 竞赛/PK/通知实时推送 |

---

## 三、目录结构

```
exam_systems/
├── src/main/java/com/exam/backend/     # Spring Boot 后端（当前实现）
│   ├── ExamBackendApplication.java     # 启动类
│   ├── client/                         # AiProperties、ArkAiClient（豆包 HTTP 客户端）
│   ├── common/                         # ApiResponse、BusinessException、ErrorCode、全局异常处理
│   ├── config/                         # Security / WebSocket / Async / Scheduler / Jpa / Ai / Web 配置
│   ├── controller/                     # REST 控制器
│   │   ├── bounty/ competition/ learning/               # 模块子包
│   ├── domain/
│   │   ├── entity/         # JPA 实体（bounty/ competition/ learning/ 子包）
│   │   ├── enums/          # 题型/难度/考试状态/会话状态/角色/通知类型
│   │   └── converter/      # JSON 属性转换器（List/Map 落库）
│   ├── dto/                # 各模块 DTO（record）
│   ├── repository/         # Spring Data 仓库（含模块子包）
│   ├── security/           # JwtProvider、JwtAuthFilter、RateLimitFilter、UserPrincipal 等
│   ├── service/            # 业务服务（含 bounty/ competition/ learning/ 子包）
│   ├── support/            # DataInitializer 种子数据
│   └── util/               # 答案比对、得分钳制、时区工具
├── src/main/resources/
│   ├── application.yml                 # 主配置（默认激活 sqlite）
│   ├── application-sqlite.yml          # 开发库（WAL）
│   ├── application-mssql.yml           # SQL Server 库
│   ├── application-prod.yml            # 生产配置
│   └── static/                         # 前端构建产物输出目录（单端口部署）
├── src/test/                           # 单元/集成/安全测试（H2 test profile）
├── frontend/                           # Vue 3 前端
│   ├── vite.config.js                  # 端口 5173、代理、build.outDir 指向后端 static
│   └── src/
│       ├── main.js                     # 入口（引入 EP 亮/暗样式、主题防 FOUC 预置）
│       ├── App.vue                     # 外壳：公开页裸渲染；登录后 玻璃顶栏 + 模块侧边栏
│       ├── style.css                   # 全局设计令牌（:root + html.dark）+ 组件换肤
│       ├── router/index.js             # 路由与守卫（public/bare/auth/role/module）
│       ├── stores/                     # auth.js、theme.js
│       ├── api/                        # index.js（axios 实例）、endpoints.js（各模块 API）
│       ├── components/                 # ThemeToggle 等
│       └── views/                      # 页面（根=考试模块；learning/ competition/ bounty/）
├── docs/postman/                       # Postman 集合
├── scripts/smoke-test.ps1              # 冒烟测试脚本
├── legacy/                             # 旧版纯 JDK 实现（已归档，不参与构建）
├── pom.xml                             # Maven 构建
└── 说明文档.md                          # 本文档
```

> 根目录的 `start.bat` / `stop.bat` / `recompile.bat` / `config.properties` / `sqlite-jdbc.jar` 属于旧版遗留，见[第十三节](#十三历史遗留说明)。

---

## 四、快速启动（开发）

> 前置条件：**JDK 17+**、**Maven 3.8+**、**Node.js 16+**

**1) 启动后端**（默认 SQLite，端口 8080）

```powershell
cd D:\Users\Documents\JavaPorject\exam_systems
mvn spring-boot:run
```

**2) 启动前端**（端口 5173，API 自动代理到 8080）

```powershell
cd D:\Users\Documents\JavaPorject\exam_systems\frontend
npm install        # 首次运行
npm run dev
```

浏览器打开 **http://localhost:5173/** 。

**3) 在线接口文档**

- Swagger UI：**http://localhost:8080/swagger-ui.html**
- OpenAPI JSON：`http://localhost:8080/v3/api-docs`
- Postman 集合：`docs/postman/exam-system.postman_collection.json`
- 冒烟测试：`powershell -File scripts/smoke-test.ps1`

**4) 演示账号**

| 账号 | 密码 | 角色 |
|------|------|------|
| teacher | 123123 | 教师 |
| student1 / student2 | 123123 | 学生 |

> 说明：以上为本地已有 `exam_system.db` 的演示密码（登录页亦有提示）。若**删除库文件重新初始化**，`DataInitializer` 会在非 prod 且用户表为空时创建同名种子账号，初始密码为 `Exam@2026!`，登录后请尽快修改。

**5) 使用流程**

```
公开首页 / → 登录 → 门户中枢 /portal（选模块）→ 模块工作区（专属侧边栏）
顶栏可随时切换模块 / 查看通知 / 切换亮暗主题，退出后回到登录页。
```

---

## 五、配置说明（多环境）

### 5.1 Profile 矩阵

| Profile | 数据库 | 用途 |
|---------|--------|------|
| `sqlite`（默认激活） | `exam_system.db`（WAL、busy_timeout=5000） | 本地开发 |
| `mssql` | SQL Server（`MSSQL_URL/USER/PASSWORD`） | 生产库切换，驱动已内置 |
| `prod` | 配合 `mssql` 使用 | 生产：`ddl-auto: validate`、关 Swagger、日志落文件 |
| `test`（src/test） | H2 内存库 | 自动化测试，禁种子/禁限流 |

启动示例：

```powershell
# 开发（默认 sqlite）
mvn spring-boot:run

# 连本机 SQL Server
mvn spring-boot:run "-Dspring-boot.run.profiles=mssql"

# 生产
java -jar exam_system.jar --spring.profiles.active=prod,mssql
```

### 5.2 环境变量

| 变量 | 必填场景 | 说明 |
|------|---------|------|
| `JWT_SECRET` | prod 必填 | 长度必须 ≥ 32 字符，否则启动即失败（JwtProvider 校验） |
| `JWT_EXPIRATION_MS` | 可选 | 默认 86400000（24h） |
| `DOUBAN_API_KEY` | 使用 AI 功能 | 火山引擎方舟 API Key；留空则 AI 调用不可用 |
| `AI_MODEL` | 可选 | 默认 `doubao-seed-1-6-250615`，可填 `ep-` 开头的推理接入点 ID |
| `MSSQL_URL` / `MSSQL_USER` / `MSSQL_PASSWORD` | mssql | 数据库连接，密码必填 |
| `SERVER_PORT` | prod 可选 | 默认 8080 |
| `CORS_ALLOWED_ORIGINS` | prod | 逗号分隔的允许前端域名 |
| `LOG_FILE` | prod 可选 | 默认 `logs/exam-backend.log` |

### 5.3 应用自定义配置（`app.*`）

```yaml
app:
  jwt:        # secret / expiration-ms
  ai:         # base-url / api-key / model / connect-timeout-ms / read-timeout-ms / max-retries
  cors:       # allowed-origins（开发默认放行 localhost:5173 / 3000）
  rate-limit: # enabled（Bucket4j 限流开关，测试环境关闭）
  seed:       # enabled（DataInitializer 种子数据开关）
  swagger:    # open
```

---

## 六、后端架构

### 6.1 请求处理链路

```
HTTP 请求
  → RateLimitFilter          （Bucket4j 令牌桶限流，可开关）
  → JwtAuthFilter            （解析 Bearer Token → SecurityContext）
  → SecurityConfig 规则       （/api/auth/** 与 swagger 放行；其余需认证；@PreAuthorize 控角色）
  → Controller               （参数校验 @Valid → 调 Service）
  → Service → Repository     （JPA / 事务边界见 6.3）
  → 统一返回 ApiResponse
```

### 6.2 统一响应与异常

所有接口返回同一信封（`common/ApiResponse.java`）：

```json
{ "code": 200, "message": "success", "data": ..., "timestamp": "2026-09-18 10:00:00" }
```

- `code = 200` 成功；错误码沿用 HTTP 语义（`common/exception/ErrorCode`）：
  `400` 参数/业务错误、`401` 认证失败、`403` 无权、`404` 不存在、`409` 冲突、`502` AI 调用失败、`500` 服务器内部错误。
- `GlobalExceptionHandler` 捕获 `BusinessException` 与校验错误并转成上述格式。

### 6.3 认证与安全

- 密码：`BCryptPasswordEncoder` 哈希存储。
- 会话：无状态 JWT（jjwt 0.12.x），登录/注册返回 `{ token, user }`，前端存 `localStorage.token` 并注入 `Authorization: Bearer <token>`。
- 角色：`teacher` / `student`，控制器用 `@PreAuthorize("hasRole('TEACHER'|'STUDENT')")` 细分；悬赏、自学等模块师生同权。
- WebSocket 握手同样经 JWT 校验（`JwtHandshakeInterceptor`）。

### 6.4 异步 AI 判分（事件驱动）

交卷主流程**不做**耗时的 AI 调用：

```
学生交卷 submit()
  → 事务内：判客观题、落 answer/result、发布 SubjectiveGradingRequested 事件
  → AFTER_COMMIT 监听器（SubjectiveGradingService）
      → 提交到专用线程池 ai-grade-*（core 2 / max 4 / queue 100，AbortPolicy）
      → 逐题调用豆包打分并写回 answer.ai_score / ai_analysis
  → 队列满被拒绝的任务：由 SubjectiveGradingSweeper 定时补偿扫描
  → AI 失败：该题兜底 0 分并保留人工复核（教师阅卷可改）
```

事务边界原则：**外部调用一律移出 DB 事务**，保证交卷响应速度与并发能力。

### 6.5 定时调度（SchedulerConfig）

- `ExamSchedulerService`：按考试时间自动发布/结束。
- `CompetitionSchedulerService`：竞赛自动开始/截止。
- `SeasonService`：赛季结算与段位归档。
- `SubjectiveGradingSweeper`：AI 判分补偿。

### 6.6 WebSocket / STOMP 实时推送

- 端点：`/ws`（同时注册 SockJS 与原生），开发经 Vite 代理转发。
- 主题约定（`RealtimePushService`）：竞赛榜 `/topic/competition/{id}`、PK 房间 `/topic/battle/{id}`、通知等个人消息走 `/user/queue/**`。
- 用途：排行榜刷新、PK 状态/作答进度、交卷结果、站内通知即时送达。

### 6.7 添加新 API（开发者参考）

1. `dto/` 定义请求/响应 record；`domain/entity/` 加实体（继承 `BaseEntity` 获得审计字段）；`repository/` 加接口。
2. `service/` 写业务（注意事务边界），`controller/` 暴露 REST 并配置 `@PreAuthorize`。
3. 前端 `api/endpoints.js` 加对应方法，页面 `import { xxxAPI }` 使用。
4. 样式一律使用 `style.css` 的语义令牌（见 7.3），禁止硬编码颜色。

---

## 七、前端架构

### 7.1 三级导航

| 层级 | 路由 | 说明 |
|------|------|------|
| 公开首页 | `/` | 产品落地页：Hero、四大模块功能展示、平台特性、使用流程、角色介绍 |
| 门户中枢 | `/portal` | 登录后模块选择页：欢迎横幅 + 四模块渐变卡片 + 快捷入口 |
| 模块工作区 | 各模块路由 | 侧边栏只显示当前模块功能；顶栏常驻模块切换器 |

`App.vue` 按 `route.meta` 决定外壳：`public/bare` 页裸渲染（无侧边栏），其余页渲染 玻璃顶栏 + 深色渐变侧栏。

### 7.2 路由与权限

`router/index.js` 的 meta 约定：

- `public` 未登录可访问；`auth` 仅要求登录（师生同权）；`role` 限定角色；
- `module` 决定所属侧边栏分组：`exam / learning / competition / bounty`；
- 守卫：未登录跳 `/login`；角色不符跳回 `/portal`；已登录访问公开页直接带入门户。

页面清单（`views/`）：

| 分组 | 页面 |
|------|------|
| 公开/门户 | Home、Login、Portal |
| 考试（教师） | Dashboard、Exams、ExamManage、Questions、Majors、Grading、Results、ResultDetail |
| 考试（学生） | MyExams、TakeExam、MyResults、ResultDetail |
| 自主学习 | LearningHome、WrongNotebook、PracticeList/Detail、StudyPlan、StudyReport |
| 竞赛 | CompetitionHome、CompetitionPlaza、CompetitionManage、CompetitionTake、CompetitionLeaderboard、PkLobby、PkTake、RankCenter、TeamCenter、StudyProfile |
| 悬赏 | BountyPlaza、BountyPublish、BountyMine、BountyDetail |

### 7.3 主题系统（亮/暗）

- **令牌驱动**：`style.css` 的 `:root` 定义语义令牌（`--ink/--surface/--canvas/--line/--brand-grad/--table-stripe/--wash-*` 等），`html.dark` 重定义同名令牌即全站换肤；Element Plus 引入官方暗色 css-vars 自动适配。
- **状态管理**：`stores/theme.js`（Pinia）——`localStorage('theme')` 持久化、首次跟随系统 `prefers-color-scheme`；`main.js` 挂载前预置 `dark` 类防 FOUC。
- **开关位置**：`components/ThemeToggle.vue`（太阳/月亮 SVG）出现在应用顶栏、公开首页导航、登录页右上角。
- **约定**：新增样式一律 `var(--令牌)`（内联 style 亦可直接写 var）；语义信息块用半透明色洗 `--wash-blue/cyan/orange`（浅深底通用）；Chart.js 图表需从 theme store 取色并 `watch(theme.dark)` 重绘。

### 7.4 API 封装与状态

- `api/index.js`：Axios 实例 `baseURL: '/api'`，请求拦截器注入 `Authorization: Bearer <localStorage.token>`，401 时清理登录态。
- `api/endpoints.js`：按模块封装 `authAPI / dictAPI / questionAPI / examAPI / resultAPI / learningAPI / competitionAPI / pkAPI / gamificationAPI / teamAPI / bountyAPI / notificationAPI / uploadAPI ...`。
- `stores/auth.js`：登录用户与 token 的 Pinia 状态（持久化到 localStorage）。

### 7.5 开发小贴士

- Vite 热更新：改 `.vue/.js` 即时生效；改 Java 代码需重启后端（`mvn spring-boot:run` 支持 devtools 自动重启）。
- 构建产物直接输出到 `src/main/resources/static`（`vite.config.js` 的 `build.outDir`），配合打 jar 实现**单端口部署**。
- 图标/表情注意 Windows 兼容：优先 SVG 图标，避免低兼容 Unicode emoji（如 U+1FA99 在 Windows 显示为方框）。

---

## 八、数据库结构

由 JPA `ddl-auto: update` 自动维护（prod 为 `validate`，表结构需预先就绪）。实体按模块分组：

### 核心（考试域）

| 实体/表 | 用途 | 关键字段 |
|---------|------|---------|
| `User` | 用户 | username、password_hash(BCrypt)、role、real_name、major |
| `Department` / `Major` / `Course` / `Chapter` | 院系-专业-课程-章节目录 | 层级外键 |
| `Question` | 题目 | content、type、difficulty、options(JSON)、answer、analysis、source、is_public、creator |
| `Exam` | 考试 | title、duration、start/end_time、status、creator |
| `ExamQuestion` | 考试-题目 | score 分值 |
| `ExamStudent` | 考试-考生 | 邀请码加入 |
| `ExamSession` | 答题会话 | start/submit_time、status |
| `Answer` | 作答记录 | student_answer、is_correct、score、manual_score/comment、ai_score/ai_analysis、needs_manual_grade |
| `Result` | 成绩 | score、total_score、ai_analysis |
| `Notification` | 站内通知 | type、title、content、read_flag |

### 自主学习域（`entity/learning/`）

`StudyPlan`（学习计划）、`PracticeSession` / `PracticeAnswer`（自由练习）、`WrongAnswerRecord`（错题本，含来源与掌握状态）、`StudyLog`（学习打卡日志）。

### 竞赛域（`entity/competition/`）

`Competition` / `CompetitionQuestion` / `CompetitionParticipant` / `CompetitionAnswer`（竞赛主数据）、`PkBattle` / `PkAnswer`（1v1 对战）、`UserPointsProfile`（积分/段位）、`UserBadge`（徽章）、`SeasonMeta` / `SeasonArchive`（赛季与归档）、`Team` / `TeamMember`（战队）。

### 悬赏域（`entity/bounty/`）

`Bounty`（悬赏单：类型、赏金、状态 open/closed）、`BountySubmission`（投稿与采纳记录）。

> 通用审计字段（创建/更新时间）由 `BaseEntity` 提供；List/Map 型字段通过 `domain/converter/` 的 JSON 转换器落库。SQLite 开发库启用 WAL 模式，读不阻塞写。

---

## 九、REST API 参考

> 前缀统一 `/api`，响应信封见 6.2。除认证接口与字典/公开查询外均需 Bearer Token。**完整与最新端点以 Swagger 为准**：`http://localhost:8080/swagger-ui.html`。

### 9.1 认证与字典

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/auth/register` | `{ username, password, role, realName, ... }` → `{ token, user }` |
| POST | `/auth/login` | `{ username, password }` → `{ token, user }` |
| GET | `/auth/me` | 当前用户信息 |
| GET | `/question-types` `/difficulties` | 题型/难度字典 |
| GET/POST/PUT/DELETE | `/departments` `/majors` `/courses` `/chapters` | 目录树维护与查询 |

### 9.2 题库与上传

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/questions?majorId=&type=&difficulty=&keyword=` | 题目列表（分页） |
| POST / PUT / DELETE | `/questions` `/questions/{id}` | 题目增删改 |
| POST | `/questions/{id}/toggle_public` | 公开共享开关 |
| POST | `/ai/generate` | AI 生成单题（见第十节） |
| POST | `/upload` | Excel 题库批量导入（multipart，≤20MB） |

### 9.3 考试管理与执行

| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST/PUT/DELETE | `/exams` `/exams/{id}` | 考试 CRUD |
| POST | `/exams/{id}/publish` `/exams/{id}/end` | 发布 / 结束 |
| GET/POST/DELETE | `/exams/{id}/questions` `/add_questions` `/remove_question/{qid}` | 试卷题目配置 |
| POST | `/exams/{id}/smart_composition` | AI 智能组卷 |
| GET/POST/DELETE | `/exams/{id}/students` `/invite` `/remove_student/{sid}` | 考生管理 |
| POST | `/exams/{id}/generate_invitation` | 生成邀请码 |
| POST | `/exam/join/{code}` | 学生凭邀请码加入 |
| GET | `/students` | 学生列表（选考生用） |
| POST | `/exam/{id}/start` | 开始答题（建会话） |
| GET | `/exam/{id}/take` | 拉取试卷（含剩余时长） |
| POST | `/exam/{id}/save_answer` | 单题自动保存 |
| POST | `/exam/{id}/submit` | 交卷（客观题即判，主观题转异步 AI） |
| POST | `/exam/{id}/report_switch` | 切屏上报 |
| GET | `/exam/{id}/report` | 本场答题报告 |

### 9.4 成绩与阅卷

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/results` `/results/{resultId}` | 成绩列表 / 详情（含逐题作答） |
| GET | `/results/me` `/results/exam/{examId}` | 我的成绩 / 某场全部成绩 |
| GET/POST | `/results/grading/{examId}[/{studentId}]` | 待阅列表 / 提交人工评分 |
| GET | `/results/analysis/{examId}` | 整场考试分析（含 AI 报告） |

### 9.5 自主学习

| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST/DELETE | `/learning/wrong-records` `/{id}/master` `/{id}` | 错题本 |
| POST/GET | `/learning/practice/start` `/practice/history` `/practice/{id}` | 练习会话 |
| POST/DELETE | `/learning/practice/{id}/answer` `/submit` `/practice/{id}` | 作答与交卷 |
| GET/POST/PUT/DELETE | `/learning/plans[...]` | 学习计划 CRUD/暂停 |
| GET | `/learning/report/overview` `/report/daily` | 学情报告 |
| POST | `/learning/ask` | AI 学习答疑 |

### 9.6 竞赛 / PK / 竞技化

| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST/PUT/DELETE | `/competitions[...]` | 竞赛 CRUD（教师） |
| POST/GET/DELETE | `/competitions/{id}/questions` `/publish` `/end` `/participants` | 赛题与状态 |
| GET | `/competitions/lobby` `/{id}/leaderboard` | 大厅 / 排行榜 |
| POST/GET | `/competitions/{id}/join` `/start` `/play` `/answer` `/finish` | 参赛全流程（学生） |
| POST/GET | `/competitions/{id}/pk` `/pk/lobby` `/pk/{battleId}/accept` `/state` `/answer` `/finish` | 1v1 PK |
| GET | `/rank/me` `/rank/seasons` `/rank/season/{s}` `/rank/archives` | 段位与赛季 |
| GET | `/badges/me` `/profile/study[/{studentId}]` | 徽章 / 学情画像 |
| GET/POST/DELETE | `/teams[...]` `/mine` `/{id}/join` `/leave` `/transfer` | 战队 |

### 9.7 征集悬赏

| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST | `/bounties` `/bounties/{id}` | 广场列表 / 详情 / 发布 |
| GET | `/bounties/mine` `/bounties/my-submissions` | 我的发布 / 我的投稿 |
| GET/POST | `/bounties/{id}/submissions` | 投稿列表 / 提交投稿 |
| POST | `/submissions/{sid}/accept` `/reject` | 采纳（CAS 抢锁）/ 驳回 |

### 9.8 通知与日志

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/notifications` `/notifications/unread` | 通知列表 / 未读数 |
| PUT | `/notifications/{id}/read` `/read_all` | 标记已读 |
| DELETE | `/notifications/{id}` `/clear_all` | 删除 |
| POST | `/logs` `/logs/vue` | 后端/前端操作日志上报 |

---

## 十、AI 能力与调用链路

### 10.1 模型接入

`ArkAiClient` 调用火山引擎方舟 Chat Completions：

```
POST https://ark.cn-beijing.volces.com/api/v3/chat/completions
Authorization: Bearer $DOUBAN_API_KEY
model: $AI_MODEL（doubao 模型名或 ep- 接入点 ID）
```

超时 `connect 30s / read 60s`，失败自动重试 `max-retries=2`；仍失败抛 `AI_CALL_FAILED(502)`。

### 10.2 应用场景

| 场景 | 入口 | 链路 |
|------|------|------|
| AI 出题 | 题库页"AI 生成题目" | `POST /api/ai/generate` → `AiService` → 返回题目+选项+答案+解析，编辑后入库 |
| 智能组卷 | 考试管理页 | `POST /api/exams/{id}/smart_composition` → 按难度/题型自动抽题组卷 |
| 主观题判分 | 学生交卷（自动触发） | 异步事件 → `SubjectiveGradingService` → 豆包逐题打分点评 → 写 `answer.ai_score/ai_analysis`；失败兜底 0 分待人工复核 |
| 考试分析 | 成绩详情"AI 分析报告" | `GET /api/results/analysis/{examId}` → 汇总作答数据 → 豆包生成优势/薄弱点/建议，存 `result.ai_analysis` |
| 学习答疑 | 自主学习"问 AI" | `POST /api/learning/ask` → 结合题目与知识点上下文回答 |
| 悬赏解析补全 | 悬赏采纳流程 | 采纳题目投稿时若缺解析，自动用 AI 补全后入库题库 |

### 10.3 使用前提

- 未配置 `DOUBAN_API_KEY` 时，除 AI 功能外系统各模块均可正常使用；AI 接口将报 502。
- 更换模型：改环境变量 `AI_MODEL` 即可，无需改代码。

---

## 十一、测试、构建与部署

### 11.1 测试

```powershell
mvn test        # 使用 H2 内存库 test profile，自动建表、禁种子、关限流
```

测试位于 `src/test/java/com/exam/backend/`：`util/`（判分工具）、`service/`、`security/`、`integration/`（登录-出题-考试全链路）。

### 11.2 生产构建（单端口部署）

```powershell
# 1) 前端构建（产物输出到 src/main/resources/static）
cd frontend; npm run build; cd ..

# 2) 后端打包（前端已随 jar 内置）
mvn package -DskipTests

# 3) 运行
java -jar target/exam_system.jar --spring.profiles.active=prod,mssql
```

访问 `http://<server>:8080/` 即为完整应用（history 路由回退已在 `SecurityConfig`/`WebConfig` 处理）。

### 11.3 生产环境清单

- [ ] `JWT_SECRET` 已设置（≥32 字符随机串）
- [ ] `MSSQL_USER` / `MSSQL_PASSWORD` / （必要时）`MSSQL_URL` 已注入
- [ ] 表结构已由 DBA/脚本预建（prod 为 `ddl-auto: validate`，只校验不建表）
- [ ] `CORS_ALLOWED_ORIGINS` 指向正式前端域名（单端口部署可留默认）
- [ ] `DOUBAN_API_KEY`（如需 AI 功能）
- [ ] 种子账号密码已修改或删除种子（`app.seed.enabled=false`）
- [ ] 建议前置 Nginx 终止 HTTPS；Swagger 生产已默认关闭

---

## 十二、常见问题与排错

### Q1. 启动报 JWT secret 相关错误

> `JwtProvider` 校验 secret 长度 ≥32。开发默认配置为空时可能报此错——设置环境变量 `JWT_SECRET`（任意 ≥32 字符随机串）后重启。

### Q2. 前端请求全部 401 / 登录成功刷新后掉线

> 检查 `localStorage.token` 是否存在、请求头是否带 `Authorization: Bearer`；后端重启后旧 JWT 仍有效（密钥未变），若改过 `JWT_SECRET` 需重新登录。

### Q3. SQLite 报 `SQLITE_BUSY` / database is locked

> 开发库已启用 `journal_mode=WAL&busy_timeout=5000`，一般可自动等锁恢复。若持续报错：确认没有其他进程（如旧实例、DB 浏览器）持有该库文件；关闭多余 `java` 进程后重启。

### Q4. 交卷后 AI 分数一直没出来

> 主观题判分是**异步**的：先看后端日志 `ai-grade-` 线程是否调用成功（Key/模型/网络），Sweeper 会对漏判记录补偿；教师阅卷页始终以人工分复核为准。

### Q5. AI 接口返回 502

> `DOUBAN_API_KEY` 未配置或方舟接入点无效/欠费。核对 `AI_MODEL` 是否为有效模型名或 `ep-` 接入点；后端日志有上游错误详情。

### Q6. 竞赛/PK 排行榜不刷新

> 确认 WebSocket 连通：浏览器 DevTools 查 `/ws` 升级请求；开发需 Vite 代理 `ws: true`（已配置）；握手带 JWT，重新登录可恢复。

### Q7. 暗色主题下某些控件"没变色"

> 项目约定所有颜色走 `style.css` 语义令牌。若新写的样式硬编码了色值则不会跟随主题——改为 `var(--surface)`、`var(--ink-2)`、`--wash-*` 等令牌即可；图表需按 7.3 约定接入主题监听。

### Q8. Windows 下某些图标显示为方框

> 避免使用较新 Unicode emoji（如 U+1FA99），统一换兼容字形或改用 SVG 图标（参考 `ThemeToggle.vue`）。

### Q9. 想完全重置数据库

> 停后端 → 删除根目录 `exam_system.db`（及 `-wal/-shm` 文件）→ 重启，`DataInitializer` 会重建表并写入种子账号（初始密码 `Exam@2026!`）。

### Q10. 页面路由 404（刷新后）

> `createWebHistory` 需要服务端回退支持。开发用 Vite dev server 无此问题；生产 jar 内置的 SPA 回退已配置，若前置 Nginx 需加 `try_files $uri /index.html;`。

---

## 十三、历史遗留说明

- **`legacy/` 目录**：项目最初为"纯 JDK `com.sun.net.httpserver` + 手写 DAO"实现（无框架、零依赖），现已整体迁移至 Spring Boot。旧代码仅作演进参考，**不参与 Maven 构建**（构建源码仅 `src/main/java`）。
- **根目录 `start.bat` / `stop.bat` / `recompile.bat` / `config.properties` / `sqlite-jdbc.jar` / `exam_system.db-shm 等`**：为旧版启动脚本与配套文件，与当前 Spring Boot 启动方式无关，请勿使用；按第四节方式启动即可。
- 旧版数据库文件 `exam_system.db` 仍被当前开发环境复用（JPA 自动补建新表），种子与演示账号见第四节。

---

**智汇学场：考试 · 自学 · 竞赛 · 悬赏，一体化智能学习闭环。**
